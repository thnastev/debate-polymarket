# Supabase project credentials. The anon key is public by design — every rule
# that protects money is enforced by RLS and the security definer functions,
# never by keeping this secret.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
