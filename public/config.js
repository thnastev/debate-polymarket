/* ============================================================
   Biser Market — the only file you need to edit.
   ============================================================

   Replace the two values below with the ones from your Supabase project:

     Supabase dashboard  ->  Project Settings  ->  API

       Project URL   ->  supabaseUrl
       anon public   ->  supabaseAnonKey   (the long one, NOT service_role)

   Keep the quotes. Save the file. That's it.

   The anon key is meant to be public — it only says which project to talk to.
   Everything that protects money is enforced inside the database.
   NEVER paste the service_role key here.
   ============================================================ */

window.BISER_CONFIG = {
  supabaseUrl:     "PASTE_YOUR_SUPABASE_URL_HERE",
  supabaseAnonKey: "PASTE_YOUR_ANON_KEY_HERE",
};
